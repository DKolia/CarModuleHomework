// Goals:
// Define a car
// Give the car properties
// Give the car methods (functions)
// Export as module to index.js
const express = require("express");
const app = express();
const req = require("car.js");

console.log(car.js + " is traveling at a final speed of " + speed);

app.listen(3000, function() {
  console.log('Launching Port 3000!');
});
